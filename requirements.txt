Django==5.2.3
django-extensions>=3.2.3
qrcode==7.4.2
Pillow==10.2.0  # Required for QR code generation
python-dateutil==2.8.2
cryptography>=41.0.0
reportlab>=4.0.0

# The pass signing functionality is provided by the cryptography package.
# Keep this version modern for security fixes unless a dependency
# explicitly requires an older range.